<!DOCTYPE html>
<html lang="zh-Hant">

<%- include("html-head"); -%>

<body>
<%- include("header"); -%>

  <section class="events">
    <div class="backgroundWords">Wanna Join?</div>
    <div class="weIntro">Latest Events</div>

    <% events.forEach(function (e, i) { %>
    <% if(i == 0) { %>
    <div class="eventBox new">
    <% } else if(i == (events.length - 1)) { %>
    <div class="eventBox last">
    <% } else { %>
    <div class="eventBox">
    <% } %>
        <div class="eventInfo">
          <p class="eventPlace"><%= e.place %></p>
          <p class="eventTime"><%= e.time %></p>
        </div>
        <div class="eventName">
          <div class="name"><%= e.title %></div>
          <a href="<%= e.url %>" class="url" target="_blank"><%= e.url %></a>
        </div>
    </div>
    <% }); %>

  </section>

<%- include("footer"); -%>
</body>
